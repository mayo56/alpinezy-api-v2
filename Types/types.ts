/**
 * Type user
 * @alpha
 */
export type User = {
    username: string; user_id: string;
    avatar_url: string; avatar_id: string;
    banner_url: string; banner_id: string;
    flags_list: Array<Flags>;
};

/**
 * Type des drapeaux (badges) de profile
 * @alpha
 */
export type Flags = {

};